# GEMM FP32 Scalar 作业

## 使用说明

把答案写在「我的答案」下面；先做「自我检查」，之后我会把批改写到「批改反馈」。

## v1 作业

### 题 1

推导题：默认 `BM=128, BN=128, BK=8` 时，为什么一个 `C` tile 每个 K tile 只需要从 global memory 读 `128*8 + 8*128` 个 float？

我的答案：

自我检查：

批改反馈：

### 题 2

判断题：`8 x 8` thread tile 为什么不一定比 `4 x 4` 永远更快？请从 register pressure 和 occupancy 两个角度回答。

我的答案：

自我检查：

批改反馈：

## v2 作业

### 题 1

概念题：`float4` 优化为什么降低的是“指令条数”，而不是默认表格里的“逻辑访存字节”？

我的答案：

自我检查：

批改反馈：

### 题 2

改错题：如果把 `smem_a[BK][BM + PAD]` 改回 `smem_a[BM][BK]`，计算阶段的索引和 bank conflict 风险会发生什么变化？

我的答案：

自我检查：

批改反馈：

## v3 作业

### 题 1

解释题：v3 的 logical bytes 和 v2 相同，为什么实测仍可能更快？

我的答案：

自我检查：

批改反馈：

### 题 2

预测题：如果把 `BK` 从 8 改成 16，可能同时带来哪些收益和风险？

我的答案：

自我检查：

批改反馈：
